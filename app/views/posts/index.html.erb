<!-- 最初にサーバーサイドでレンダリングされる部分 -->
<counter-app></counter-app>
<% @posts.each do |post| %>
  <div class="post">
    <%= link_to post_url(post.id) do %>
      <h2><%= post.title %></h2>
      <p><%= post.body %></p>
    <% end %>
  </div>
<% end %>
<on-boarding></on-boaring>
